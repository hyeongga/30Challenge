import { useState } from "react";

const TodoCard = ({}) => {
  const [todoIsDone, setTodoIsDone] = useState(isDone);

  return (
    <>
      <button></button>
    </>
  );
};
export default TodoCard;
